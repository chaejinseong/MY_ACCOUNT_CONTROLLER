export default function MailRulesPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-xl font-semibold">메일 규칙</h1>
      <p className="text-sm text-neutral-500">
        Gmail 계정 연결 및 규칙(발신자/제목/본문 키워드 등) 설정 화면은 백엔드 로직이
        준비되면 이어서 구현합니다.
      </p>
    </div>
  );
}
